import React, { useState } from "react";
import WeatherDisplay from "./WeatherDisplay";

const WeatherApp = () => {
  return (
    <div>
      <WeatherDisplay/>
    </div>
  );
};

export default WeatherApp;
